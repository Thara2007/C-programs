#include <stdio.h>

int main() {
    int num = 1234, sum = 0, rem;
    while(num != 0) {
        rem = num % 10;
        sum = sum + rem;
        num = num / 10;
    }
    printf("Sum of digits = %d\n", sum);
    return 0;
}
